{
  "name": "@stormstack/billing",
  "version": "0.1.0",
  "description": "Storm Stack — Stripe billing plugin (subscriptions, invoices, webhooks)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "peerDependencies": {
    "@stormstack/core": "^0.1.0"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "zod": "^3.22.0"
  }
}
