{
  "name": "@storm/plugin-stripe",
  "version": "0.1.0",
  "description": "Storm Stack — Stripe billing plugin (subscriptions, invoices, webhooks)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@storm/core": "^0.1.0"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "zod": "^3.22.0"
  }
}
