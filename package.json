{
  "name": "storm-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Storm Stack — React/Node plugin framework for SaaS applications",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "pack:all": "npm pack --dry-run --workspace packages/core && npm pack --dry-run --workspace packages/react && npm pack --dry-run --workspace packages/testing && npm pack --dry-run --workspace packages/cli && npm pack --dry-run --workspace packages/plugin-auth && npm pack --dry-run --workspace packages/plugin-auth-social && npm pack --dry-run --workspace packages/plugin-crm && npm pack --dry-run --workspace packages/plugin-ticketing && npm pack --dry-run --workspace packages/plugin-stripe && npm pack --dry-run --workspace packages/create-storm-app",
    "smoke:create-app": "node scripts/smoke-create-app.mjs",
    "catalog:check": "node scripts/check-catalogs.mjs",
    "metadata:check": "node scripts/check-package-metadata.mjs",
    "version:check": "node scripts/sync-internal-versions.mjs --check",
    "version:sync": "node scripts/sync-internal-versions.mjs --write && npm install --package-lock-only --ignore-scripts",
    "audit:prod": "npm audit --omit=dev --audit-level=moderate",
    "release:check": "npm run version:check && npm run catalog:check && npm run metadata:check && npm run audit:prod && npm run lint && npm run typecheck && npm run build && npm test && npm run smoke:create-app && npm run pack:all",
    "publish:dry-run": "npm run release:check && node scripts/publish-all.mjs --dry-run",
    "publish:all": "npm run release:check && node scripts/publish-all.mjs --live",
    "version:all": "npm version patch --workspaces --no-git-tag-version && npm version patch --no-git-tag-version && npm run version:sync"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "packageManager": "npm@10.0.0"
}
