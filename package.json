{
  "name": "storm-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Storm Stack — React/Node plugin framework for SaaS applications",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "pack:all": "npm pack --dry-run --workspace packages/core && npm pack --dry-run --workspace packages/react && npm pack --dry-run --workspace packages/testing && npm pack --dry-run --workspace packages/cli && npm pack --dry-run --workspace packages/plugin-auth && npm pack --dry-run --workspace packages/plugin-auth-social && npm pack --dry-run --workspace packages/plugin-crm && npm pack --dry-run --workspace packages/plugin-ticketing && npm pack --dry-run --workspace packages/plugin-stripe && npm pack --dry-run --workspace packages/create-storm-app",
    "release:check": "npm run lint && npm run typecheck && npm test && npm run build && npm run pack:all",
    "publish:all": "npm run build && npm publish --workspace packages/core && npm publish --workspace packages/react && npm publish --workspace packages/testing && npm publish --workspace packages/cli && npm publish --workspace packages/plugin-auth && npm publish --workspace packages/plugin-auth-social && npm publish --workspace packages/plugin-crm && npm publish --workspace packages/plugin-ticketing && npm publish --workspace packages/plugin-stripe && npm publish --workspace packages/create-storm-app",
    "version:all": "npm version patch --workspaces --no-git-tag-version"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.0.0"
}
